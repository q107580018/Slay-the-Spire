---
phase: 03-奖励与经济统一
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/slay_the_spire/use_cases/reward_actions.py
  - src/slay_the_spire/use_cases/apply_reward.py
  - tests/use_cases/test_apply_reward.py
autonomous: true
requirements: [REWARD-01, REWARD-03]
must_haves:
  truths:
    - "奖励系统可识别并执行统一的 reward action 协议，不再只支持零散前缀。"
    - "升级/移除/复制/转换/跳过等奖励动作可以通过统一 apply 入口得到确定结果。"
  artifacts:
    - path: "src/slay_the_spire/use_cases/reward_actions.py"
      provides: "Reward action 协议与解析函数"
    - path: "src/slay_the_spire/use_cases/apply_reward.py"
      provides: "基于统一协议的奖励应用引擎"
    - path: "tests/use_cases/test_apply_reward.py"
      provides: "统一奖励动作的行为回归测试"
  key_links:
    - from: "src/slay_the_spire/use_cases/reward_actions.py"
      to: "src/slay_the_spire/use_cases/apply_reward.py"
      via: "parse_reward_action -> apply_reward_action"
      pattern: "parse_reward_action\\(|apply_reward_action\\("
    - from: "src/slay_the_spire/use_cases/apply_reward.py"
      to: "tests/use_cases/test_apply_reward.py"
      via: "新增动作测试"
      pattern: "test_.*(transform|duplicate|upgrade|remove|skip)"
---

<objective>
统一奖励协议与 apply 核心，使后续入口改造都能复用同一执行链。

Purpose: 先建立稳定“合同层”，避免在 shop/event/rest/neow 各自复制奖励逻辑。
Output: reward action 协议模块 + apply 引擎扩展 + 对应测试。
</objective>

<execution_context>
@/Users/qiuwen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/qiuwen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-奖励与经济统一/03-RESEARCH.md
@src/slay_the_spire/use_cases/apply_reward.py
@tests/use_cases/test_apply_reward.py

<interfaces>
From src/slay_the_spire/use_cases/apply_reward.py:
```python
def apply_reward(
    *, run_state: RunState, reward_id: str, registry: ContentProviderPort
) -> RunState: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 建立统一奖励动作协议模块</name>
  <read_first>
    - src/slay_the_spire/use_cases/apply_reward.py
    - src/slay_the_spire/use_cases/opening_flow.py
    - src/slay_the_spire/use_cases/shop_action.py
    - src/slay_the_spire/use_cases/rest_action.py
    - src/slay_the_spire/use_cases/event_action.py
    - .planning/phases/03-奖励与经济统一/03-RESEARCH.md
  </read_first>
  <files>src/slay_the_spire/use_cases/reward_actions.py</files>
  <acceptance_criteria>
    - 文件导出 `RewardAction`（dataclass）与 `parse_reward_action(reward_id: str)`.
    - `parse_reward_action` 至少支持 `gold/relic/potion/card/card_offer/event/remove/upgrade/transform/duplicate/skip` 十类动作。
    - `uv run python -c "from slay_the_spire.use_cases.reward_actions import parse_reward_action; print(parse_reward_action('gold:10').kind)"` 输出 `gold`。
  </acceptance_criteria>
  <action>创建 `reward_actions.py`，定义 `RewardAction(kind: str, payload: dict[str, str|int|bool])` 和严格解析器；对未知前缀保留 `kind='noop'` 且 payload 含原始 `reward_id`，禁止抛异常中断流程。</action>
  <verify>
    <automated>uv run python -c "from slay_the_spire.use_cases.reward_actions import parse_reward_action; assert parse_reward_action('upgrade:strike#1').kind == 'upgrade'"</automated>
  </verify>
  <done>统一协议文件可被导入，所有已约定奖励前缀均可解析为结构化动作。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 扩展 apply_reward 为统一动作执行器</name>
  <read_first>
    - src/slay_the_spire/use_cases/reward_actions.py
    - src/slay_the_spire/use_cases/apply_reward.py
    - src/slay_the_spire/domain/models/run_state.py
    - src/slay_the_spire/domain/models/cards.py
  </read_first>
  <files>src/slay_the_spire/use_cases/apply_reward.py</files>
  <behavior>
    - Test 1: `transform` 动作会替换目标卡并保持实例后缀连续可追踪。
    - Test 2: `duplicate` 动作会复制目标卡为新实例 ID，不覆盖原卡。
    - Test 3: `remove/upgrade/skip` 动作不会破坏已有 gold/relic/card/potion 语义。
  </behavior>
  <acceptance_criteria>
    - 新增 `apply_reward_action(...)`，`apply_reward(...)` 内先调用 `parse_reward_action(...)`。
    - 对 `noop` 或非法 payload 返回原 `run_state`，并保持 deterministic（同输入同输出）。
    - `rg -n "def apply_reward_action|parse_reward_action" src/slay_the_spire/use_cases/apply_reward.py` 有命中。
  </acceptance_criteria>
  <action>在 `apply_reward.py` 中引入动作分派表，保留现有 `gold/relic/card_offer/potion/card` 行为不变；新增 `remove/upgrade/transform/duplicate/skip` 处理分支并复用已有 `_next_instance_id` 与 registry 校验。</action>
  <verify>
    <automated>uv run pytest tests/use_cases/test_apply_reward.py -k "apply_reward" -x</automated>
  </verify>
  <done>统一执行器可覆盖旧前缀奖励与新增动作奖励，且历史奖励测试保持通过。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 补齐统一动作的最小回归测试矩阵</name>
  <read_first>
    - tests/use_cases/test_apply_reward.py
    - src/slay_the_spire/use_cases/apply_reward.py
    - src/slay_the_spire/use_cases/reward_actions.py
  </read_first>
  <files>tests/use_cases/test_apply_reward.py</files>
  <behavior>
    - Test 1: `duplicate:*` 会新增一张同 ID 卡实例。
    - Test 2: `transform:*` 会把目标卡替换为可用目标卡。
    - Test 3: `skip:*` 为 no-op，不变更牌组/金币/遗物。
  </behavior>
  <acceptance_criteria>
    - 新增至少 6 条测试，覆盖 `remove/upgrade/transform/duplicate/skip/noop`。
    - `uv run pytest tests/use_cases/test_apply_reward.py -k "duplicate or transform or skip or remove or upgrade" -x` 通过。
    - 新测试命名遵循 `test_apply_reward_*`。
  </acceptance_criteria>
  <action>在现有 `test_apply_reward.py` 追加统一动作测试，使用本地 helper `_run_state()` 与 `_content_provider()`；不要新增脆弱的随机断言，统一采用固定 seed 或直接构造 deck。</action>
  <verify>
    <automated>uv run pytest tests/use_cases/test_apply_reward.py -x</automated>
  </verify>
  <done>统一动作行为具备可重复执行的测试护栏，后续入口改造可安全复用。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| menu/input -> reward parser | 玩家输入动作字符串进入奖励解析器 |
| reward parser -> run_state mutation | 解析后的动作驱动状态变更 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | T | `parse_reward_action` | mitigate | 对未知/非法 reward_id 返回 `noop`，避免异常导致流程中断。 |
| T-03-02 | D | `apply_reward_action` | mitigate | 非法 payload 不抛出未捕获异常，返回原状态并由调用方提供提示。 |
| T-03-03 | E | `apply_reward_action` | mitigate | 所有 card/relic/potion 入口必须先走 registry 校验，拒绝不存在 ID。 |
</threat_model>

<verification>
- `uv run pytest tests/use_cases/test_apply_reward.py -x`
- `uv run pytest -m guardrail tests/use_cases/test_apply_reward.py -x`
</verification>

<success_criteria>
- 非战斗奖励相关动作具备统一解析与执行入口。
- 旧奖励语义无回归，新动作至少有 6 条直接测试覆盖。
</success_criteria>

<output>
After completion, create `.planning/phases/03-奖励与经济统一/03-01-SUMMARY.md`
</output>
