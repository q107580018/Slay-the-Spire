---
phase: 01-护栏与交付契约
plan: "03"
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - README.md
  - tests/docs/test_readme.py
autonomous: true
requirements: [GUARD-03]
must_haves:
  truths:
    - "README documents uv run pytest -m guardrail and when to use it."
    - "README documents the failure meaning for guardrail content reachability checks."
    - "README contains a content batch checklist covering content, registry, domain/use case, session, presentation/Textual, and README updates."
  artifacts:
    - path: "README.md"
      provides: "developer-facing guardrail command and content batch checklist"
      contains: "uv run pytest -m guardrail"
    - path: "tests/docs/test_readme.py"
      provides: "README drift test"
      contains: "test_readme_documents_guardrail_command_and_content_batch_checklist"
  key_links:
    - from: "tests/docs/test_readme.py"
      to: "README.md"
      via: "read_text assertions"
      pattern: "uv run pytest -m guardrail"
    - from: "README.md"
      to: "Phase 1 content batch delivery contract"
      via: "checklist exact strings"
      pattern: "presentation/Textual"
---

<objective>
Document the GUARD-03 delivery contract in README and lock the key README strings with tests.

Purpose: Keep future content batches from becoming JSON-only changes by making the acceptance checklist part of the maintained developer docs and guardrail test suite.
Output: README guardrail section plus README test assertions.
</objective>

<execution_context>
@/Users/qiuwen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/qiuwen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-护栏与交付契约/01-CONTEXT.md
@.planning/phases/01-护栏与交付契约/01-RESEARCH.md
@README.md
@tests/docs/test_readme.py

<interfaces>
Existing README test pattern:
```python
from pathlib import Path

def test_readme_mentions_batch_five_relic_coverage_without_claiming_full_completion() -> None:
    readme = (Path(__file__).resolve().parents[2] / "README.md").read_text(encoding="utf-8")
    assert "implementation_status" in readme
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add README guardrail command and batch checklist</name>
  <files>README.md</files>
  <read_first>
    - README.md
    - .planning/phases/01-护栏与交付契约/01-CONTEXT.md
    - .planning/phases/01-护栏与交付契约/01-RESEARCH.md
  </read_first>
  <action>Update README in the existing testing/development area. Under `## 测试建议`, add a subsection titled `### Guardrail 回归护栏` with this exact command block:
```bash
uv run pytest -m guardrail
```
The text must state that the guardrail subset covers `session 菜单模式`、`跨幕推进`、`reward generate/apply`、`effect queue/hook 时序`、`save/load round-trip`、`内容已录入 vs 可触达校验`. It must also state that Phase 1 does not use a `coverage 百分比阈值` and does not require a full three-act E2E.

Add a subsection titled `### 新增内容批次验收清单` with checklist bullets containing these exact strings:
- `` `content/` ``
- `registry/content validation`
- `domain/use case`
- `session route`
- `presentation/Textual`
- `README`
- `` `implementation_status` ``
- `不进入随机投放池`
- `uv run pytest -m guardrail`

Also update the existing relic coverage sentence if needed so it no longer claims a stale placeholder count as a maintained fact. Keep the statement that placeholder relics use `implementation_status` and that filtering is based on runtime random pools, not hand-editing `src/slay_the_spire/data/content/`.</action>
  <verify>
    <automated>rg -n 'Guardrail 回归护栏|uv run pytest -m guardrail|新增内容批次验收清单|registry/content validation|presentation/Textual|不进入随机投放池' README.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'Guardrail 回归护栏' README.md` prints one match.
    - `rg -n 'uv run pytest -m guardrail' README.md` prints at least one match.
    - `rg -n 'session 菜单模式|跨幕推进|reward generate/apply|effect queue/hook 时序|save/load round-trip|内容已录入 vs 可触达校验' README.md` prints matches for every listed phrase.
    - `rg -n 'registry/content validation|domain/use case|session route|presentation/Textual|不进入随机投放池' README.md` prints matches for every listed phrase.
  </acceptance_criteria>
  <done>README contains the guardrail command, guardrail scope, failure interpretation, and content batch checklist required by D-09 through D-16.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add README guardrail drift test</name>
  <files>tests/docs/test_readme.py</files>
  <read_first>
    - tests/docs/test_readme.py
    - README.md
    - .planning/phases/01-护栏与交付契约/01-CONTEXT.md
    - .planning/phases/01-护栏与交付契约/01-RESEARCH.md
  </read_first>
  <behavior>
    - The test fails if `uv run pytest -m guardrail` disappears from README.
    - The test fails if any required checklist link disappears from README.
    - The test is included in the guardrail marker subset.
  </behavior>
  <action>In `tests/docs/test_readme.py`, add `import pytest` if absent. Add `@pytest.mark.guardrail` test `test_readme_documents_guardrail_command_and_content_batch_checklist` that reads README exactly like the existing test and asserts these exact strings are present:
```python
required_fragments = [
    "uv run pytest -m guardrail",
    "session 菜单模式",
    "跨幕推进",
    "reward generate/apply",
    "effect queue/hook 时序",
    "save/load round-trip",
    "内容已录入 vs 可触达校验",
    "`content/`",
    "registry/content validation",
    "domain/use case",
    "session route",
    "presentation/Textual",
    "`implementation_status`",
    "不进入随机投放池",
]
```
Loop through `required_fragments` and assert each fragment is in `readme`. Do not assert a relic placeholder count such as `102` or `98`; the research says the count is unstable and should not become a brittle docs test.</action>
  <verify>
    <automated>uv run pytest -m guardrail tests/docs/test_readme.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'test_readme_documents_guardrail_command_and_content_batch_checklist' tests/docs/test_readme.py` prints one match.
    - `rg -n '@pytest\\.mark\\.guardrail' tests/docs/test_readme.py` prints one match.
    - `rg -n '102|98' tests/docs/test_readme.py` exits non-zero for the new test body.
    - `uv run pytest -m guardrail tests/docs/test_readme.py -q` exits 0.
  </acceptance_criteria>
  <done>README command/checklist claims are guarded by an automated pytest marker test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README -> developer behavior | Local developer docs influence what files and tests future content batches update. |
| docs test -> README | Test assertions constrain README claims to prevent drift. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Repudiation | README checklist | mitigate | Add exact README test fragments for command scope and content batch checklist. |
| T-01-03-02 | Tampering | documentation claims | mitigate | Avoid brittle placeholder counts and assert stable command/checklist claims instead. |
| T-01-03-03 | Information Disclosure | README | accept | README contains only local development commands and content workflow guidance; no secrets or external services are in scope. |
</threat_model>

<verification>
Run:
```bash
rg -n 'Guardrail 回归护栏|uv run pytest -m guardrail|新增内容批次验收清单|registry/content validation|presentation/Textual|不进入随机投放池' README.md
uv run pytest -m guardrail tests/docs/test_readme.py -q
```
</verification>

<success_criteria>
GUARD-03 is satisfied when README documents the guardrail command, failure meaning, and full content batch checklist, and `tests/docs/test_readme.py` fails if those exact stable strings drift.
</success_criteria>

<output>
After completion, create `.planning/phases/01-护栏与交付契约/01-03-SUMMARY.md`.
</output>
