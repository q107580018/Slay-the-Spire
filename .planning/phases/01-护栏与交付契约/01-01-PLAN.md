---
phase: 01-护栏与交付契约
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - tests/app/test_session.py
  - tests/e2e/test_two_act_smoke.py
  - tests/use_cases/test_apply_reward.py
  - tests/domain/test_effect_resolver.py
  - tests/use_cases/test_save_load.py
autonomous: true
requirements: [GUARD-01]
must_haves:
  truths:
    - "开发者可以运行 uv run pytest -m guardrail 选择关键回归护栏。"
    - "guardrail 子集覆盖 session 菜单模式、跨幕推进、reward generate/apply、effect queue/hook 时序和 save/load round-trip。"
    - "Phase 1 不引入 coverage 百分比阈值，也不把短路径 smoke 扩成完整三幕通关。"
  artifacts:
    - path: "pyproject.toml"
      provides: "pytest guardrail marker registration"
      contains: "guardrail: critical regression contract for session/reward/effect/save-load/content reachability"
    - path: "tests/domain/test_effect_resolver.py"
      provides: "effect queue/hook timing guardrail tests"
      contains: "@pytest.mark.guardrail"
    - path: "tests/e2e/test_two_act_smoke.py"
      provides: "short fixed-path cross-act smoke guardrail"
      contains: "@pytest.mark.guardrail"
  key_links:
    - from: "pyproject.toml"
      to: "pytest -m guardrail"
      via: "[tool.pytest.ini_options].markers"
      pattern: "guardrail: critical regression contract"
    - from: "tests/domain/test_effect_resolver.py"
      to: "src/slay_the_spire/domain/effects/effect_resolver.py"
      via: "resolve_next_effect / resolve_effect_queue tests"
      pattern: "test_effects_append_to_queue_tail_in_order"
---

<objective>
Create the GUARD-01 pytest selector and mark the existing critical-path tests that define the first regression contract.

Purpose: Give later phases one stable, fast command for the highest-risk session/reward/effect/save-load paths without adding a coverage threshold or full three-act E2E.
Output: Registered pytest marker plus function-level guardrail annotations on representative existing tests.
</objective>

<execution_context>
@/Users/qiuwen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/qiuwen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-护栏与交付契约/01-CONTEXT.md
@.planning/phases/01-护栏与交付契约/01-RESEARCH.md
@.planning/codebase/TESTING.md
@pyproject.toml
@tests/app/test_session.py
@tests/e2e/test_two_act_smoke.py
@tests/use_cases/test_apply_reward.py
@tests/domain/test_effect_resolver.py
@tests/use_cases/test_save_load.py

<interfaces>
Pytest config currently has:
```toml
[tool.pytest.ini_options]
pythonpath = ["src"]
```

Guardrail command required by D-13:
```bash
uv run pytest -m guardrail
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register the guardrail marker</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml
    - .planning/phases/01-护栏与交付契约/01-CONTEXT.md
    - .planning/phases/01-护栏与交付契约/01-RESEARCH.md
  </read_first>
  <action>Add a `markers` array under `[tool.pytest.ini_options]` while preserving `pythonpath = ["src"]`. The exact final block must contain:
```toml
[tool.pytest.ini_options]
pythonpath = ["src"]
markers = [
    "guardrail: critical regression contract for session/reward/effect/save-load/content reachability",
]
```
Do not add pytest-cov, coverage settings, shell scripts, or a custom test runner; D-13 locks the pytest marker entry point and D-15 excludes coverage thresholds.</action>
  <verify>
    <automated>uv run pytest --markers | rg "guardrail: critical regression contract"</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'markers = \\[' pyproject.toml` prints one match under `[tool.pytest.ini_options]`.
    - `rg -n 'guardrail: critical regression contract for session/reward/effect/save-load/content reachability' pyproject.toml` prints one match.
    - `uv run pytest --markers | rg 'guardrail: critical regression contract'` exits 0.
  </acceptance_criteria>
  <done>`pytest` recognizes the `guardrail` marker without emitting an unknown-marker warning.</done>
</task>

<task type="auto">
  <name>Task 2: Mark representative GUARD-01 tests</name>
  <files>tests/app/test_session.py, tests/e2e/test_two_act_smoke.py, tests/use_cases/test_apply_reward.py, tests/domain/test_effect_resolver.py, tests/use_cases/test_save_load.py</files>
  <read_first>
    - tests/app/test_session.py
    - tests/e2e/test_two_act_smoke.py
    - tests/use_cases/test_apply_reward.py
    - tests/domain/test_effect_resolver.py
    - tests/use_cases/test_save_load.py
    - .planning/phases/01-护栏与交付契约/01-CONTEXT.md
    - .planning/phases/01-护栏与交付契约/01-RESEARCH.md
  </read_first>
  <action>Add function-level `@pytest.mark.guardrail` decorators to the existing tests named below. If a file does not already import pytest, add `import pytest` near existing imports. Mark only these representative tests; do not use module-level `pytestmark` on large files.

Mark in `tests/app/test_session.py`:
- `test_root_load_enters_save_list_menu`
- `test_load_select_can_restore_specific_save_from_root_menu`
- `test_root_next_room_with_wing_boots_enters_select_next_room_menu`
- `test_route_menu_choice_without_wing_boots_rejects_unlinked_next_room`

Mark in `tests/e2e/test_two_act_smoke.py`:
- `test_act1_boss_reward_transitions_into_act2_start_room`
- `test_act2_boss_reward_finishes_run_with_victory`

Mark in `tests/use_cases/test_apply_reward.py`:
- `test_apply_reward_adds_gold_to_run_state`
- `test_apply_reward_accepts_card_offer_reward_ids`
- `test_generate_boss_rewards_returns_three_unique_relics`
- `test_generate_combat_rewards_returns_gold_and_three_unique_card_offers`
- `test_generate_combat_rewards_elite_gold_stays_in_25_to_35_range_and_grants_relic`

Mark in `tests/domain/test_effect_resolver.py`:
- `test_effects_append_to_queue_tail_in_order`
- `test_resolver_never_recurses_synchronously`
- `test_dead_targets_become_noop_effects`
- `test_on_enemy_defeated_enqueues_before_on_combat_end`
- `test_on_combat_end_is_enqueued_only_after_defeat_hook_resolves`
- `test_on_combat_end_fires_once_even_if_multiple_enemies_die`

Mark in `tests/use_cases/test_save_load.py`:
- `test_save_load_round_trips_extended_combat_state`
- `test_save_load_round_trips_relic_sequence_state`
- `test_save_load_preserves_boss_chest_room_state`

These selections implement D-01, D-02, D-03 and D-04 while avoiding a full three-act E2E or broad module-level marker.</action>
  <verify>
    <automated>uv run pytest -m guardrail tests/app/test_session.py tests/e2e/test_two_act_smoke.py tests/use_cases/test_apply_reward.py tests/domain/test_effect_resolver.py tests/use_cases/test_save_load.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n '@pytest\\.mark\\.guardrail' tests/app/test_session.py tests/e2e/test_two_act_smoke.py tests/use_cases/test_apply_reward.py tests/domain/test_effect_resolver.py tests/use_cases/test_save_load.py` prints at least 20 matches.
    - `rg -n 'pytestmark = pytest\\.mark\\.guardrail' tests/app/test_session.py tests/e2e/test_two_act_smoke.py tests/use_cases/test_apply_reward.py tests/domain/test_effect_resolver.py tests/use_cases/test_save_load.py` exits non-zero.
    - `uv run pytest -m guardrail tests/app/test_session.py tests/e2e/test_two_act_smoke.py tests/use_cases/test_apply_reward.py tests/domain/test_effect_resolver.py tests/use_cases/test_save_load.py -q` exits 0.
  </acceptance_criteria>
  <done>The guardrail subset covers session menu/load paths, cross-act boss reward smoke, reward generate/apply, effect queue/hook timing, and save/load round-trip behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer shell -> pytest config | Local developer command selects tests through repository-controlled marker config. |
| test code -> local save/content fixtures | Tests read local fixtures and may create temporary save files through `tmp_path`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Tampering | `pyproject.toml` pytest marker | mitigate | Register a named marker and verify with `uv run pytest --markers`; do not add unreviewed shell runners. |
| T-01-01-02 | Repudiation | guardrail test selector | mitigate | Use function-level `@pytest.mark.guardrail` on explicit tests so `rg` and pytest output show what is in the contract. |
| T-01-01-03 | Denial of Service | overly broad guardrail subset | mitigate | Avoid module-level markers on large files and verify the selected subset directly with `uv run pytest -m guardrail ... -q`. |
</threat_model>

<verification>
Run:
```bash
uv run pytest --markers | rg "guardrail: critical regression contract"
uv run pytest -m guardrail tests/app/test_session.py tests/e2e/test_two_act_smoke.py tests/use_cases/test_apply_reward.py tests/domain/test_effect_resolver.py tests/use_cases/test_save_load.py -q
```
</verification>

<success_criteria>
GUARD-01 is satisfied when `uv run pytest -m guardrail` can select a critical-path subset that includes session menu/load, cross-act smoke, reward generate/apply, effect timing, and save/load tests, with no coverage threshold or full three-act E2E added in this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/01-护栏与交付契约/01-01-SUMMARY.md`.
</output>
