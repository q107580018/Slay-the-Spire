---
phase: 01-护栏与交付契约
plan: "02"
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - src/slay_the_spire/use_cases/start_run.py
  - src/slay_the_spire/use_cases/opening_flow.py
  - tests/use_cases/test_start_run.py
  - tests/content/test_registry_validation.py
autonomous: true
requirements: [GUARD-02]
must_haves:
  truths:
    - "内容校验能区分 registry 已录入内容和运行时可触达内容。"
    - "placeholder 遗物会在报告中单独出现。"
    - "placeholder 遗物不会进入 common/uncommon/rare/shop/boss relic sequences 或 Neow 随机遗物池。"
  artifacts:
    - path: "src/slay_the_spire/use_cases/start_run.py"
      provides: "runtime relic sequence filtering"
      contains: "implementation_status"
    - path: "src/slay_the_spire/use_cases/opening_flow.py"
      provides: "Neow random relic filtering"
      contains: "implementation_status"
    - path: "tests/use_cases/test_start_run.py"
      provides: "start_run and Neow placeholder filtering tests"
      contains: "test_start_new_run_excludes_placeholder_relics_from_random_sequences"
    - path: "tests/content/test_registry_validation.py"
      provides: "loaded-vs-reachable content report helper and guardrail assertions"
      contains: "ReachabilityRow"
  key_links:
    - from: "src/slay_the_spire/use_cases/start_run.py"
      to: "tests/use_cases/test_start_run.py"
      via: "start_new_run relic_sequences"
      pattern: "implementation_status.*placeholder"
    - from: "src/slay_the_spire/use_cases/opening_flow.py"
      to: "tests/use_cases/test_start_run.py"
      via: "_choose_relic_id"
      pattern: "neow.*implementation_status"
    - from: "tests/content/test_registry_validation.py"
      to: "content/"
      via: "StarterContentProvider and runtime pool metadata"
      pattern: "loaded_not_reachable"
---

<objective>
Implement the GUARD-02 content reachability contract and explicitly filter placeholder relics out of runtime random reward pools.

Purpose: Prevent JSON-only content or placeholder relics from being counted as player-deliverable while still making placeholders visible in a readable report.
Output: Scoped runtime filters for relic sequences and Neow relic selection, plus reachability report helper and guardrail tests.
</objective>

<execution_context>
@/Users/qiuwen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/qiuwen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-护栏与交付契约/01-CONTEXT.md
@.planning/phases/01-护栏与交付契约/01-RESEARCH.md
@.planning/codebase/TESTING.md
@src/slay_the_spire/use_cases/start_run.py
@src/slay_the_spire/use_cases/opening_flow.py
@tests/use_cases/test_start_run.py
@tests/content/test_registry_validation.py
@src/slay_the_spire/content/catalog.py
@src/slay_the_spire/content/provider.py
@src/slay_the_spire/content/registries.py

<interfaces>
Existing runtime relic pools in `src/slay_the_spire/use_cases/start_run.py`:
```python
_RELIC_SEQUENCE_POOL_IDS = ("common", "uncommon", "rare", "shop", "boss")

def _is_rewardable_relic(*, relic, character_id: str, pool_id: str) -> bool:
    if pool_id not in relic.pools:
        return False
    return not relic.owner_character_ids or character_id in relic.owner_character_ids
```

Existing Neow relic selection in `src/slay_the_spire/use_cases/opening_flow.py`:
```python
if "neow" in relic.pools and (
    not relic.owner_character_ids
    or run_state.character_id in relic.owner_character_ids
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Filter placeholder relics from random runtime pools</name>
  <files>src/slay_the_spire/use_cases/start_run.py, src/slay_the_spire/use_cases/opening_flow.py, tests/use_cases/test_start_run.py</files>
  <read_first>
    - src/slay_the_spire/use_cases/start_run.py
    - src/slay_the_spire/use_cases/opening_flow.py
    - tests/use_cases/test_start_run.py
    - content/relics/common_relics.json
    - content/relics/boss_relics.json
    - .planning/phases/01-护栏与交付契约/01-CONTEXT.md
    - .planning/phases/01-护栏与交付契约/01-RESEARCH.md
  </read_first>
  <behavior>
    - `start_new_run("ironclad", seed=7, registry=provider).relic_sequences` contains no relic whose `implementation_status == "placeholder"`.
    - `_choose_relic_id` for Neow never chooses a placeholder relic, even when placeholder relic IDs sort after implemented relic IDs.
    - Existing auto-inclusion tests use `implementation_status="implemented"` when they expect a test relic to enter runtime sequences.
    - Placeholder relics are still loaded in the registry; this task only filters runtime random reward pools.
  </behavior>
  <action>In `src/slay_the_spire/use_cases/start_run.py`, update `_is_rewardable_relic` so it returns `False` when `relic.implementation_status == "placeholder"` before checking `relic.pools` and `owner_character_ids`.

In `src/slay_the_spire/use_cases/opening_flow.py`, update `_choose_relic_id` so the list comprehension excludes `relic.implementation_status == "placeholder"` while keeping the existing `"neow" in relic.pools` and owner-character checks.

In `tests/use_cases/test_start_run.py`, update existing fixture payloads in `test_start_new_run_auto_includes_new_relic_entries_by_pool` and `test_neow_random_relic_selection_uses_neow_pool_membership` from `"implementation_status": "placeholder"` to `"implementation_status": "implemented"` because those tests intentionally verify inclusion. Then add `@pytest.mark.guardrail` test `test_start_new_run_excludes_placeholder_relics_from_random_sequences`:
```python
def test_start_new_run_excludes_placeholder_relics_from_random_sequences() -> None:
    provider = _content_provider()
    run_state = start_new_run("ironclad", seed=7, registry=provider)
    offenders = [
        f"{pool_id}:{relic_id}"
        for pool_id, relic_ids in run_state.relic_sequences.items()
        for relic_id in relic_ids
        if provider.relics().get(relic_id).implementation_status == "placeholder"
    ]
    assert not offenders, "placeholder relics in random pools:\\n" + "\\n".join(offenders[:50])
```
Also add a Neow-specific guardrail test that appends two Neow relics to a copied content root: `zz_test_neow_placeholder_relic` with `"implementation_status": "placeholder"` and `zy_test_neow_implemented_relic` with `"implementation_status": "implemented"`, both with `"pools": ["common", "neow"]`. Use the existing `_PickLastRng` pattern from `test_neow_random_relic_selection_uses_neow_pool_membership` and assert `_choose_relic_id(...) == "zy_test_neow_implemented_relic"`.

Do not silently change root `content/` metadata to make tests pass; this is a runtime filter plus tests per D-06 and D-12.</action>
  <verify>
    <automated>uv run pytest -m guardrail tests/use_cases/test_start_run.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'implementation_status == "placeholder"' src/slay_the_spire/use_cases/start_run.py src/slay_the_spire/use_cases/opening_flow.py` prints matches in both source files.
    - `rg -n 'test_start_new_run_excludes_placeholder_relics_from_random_sequences|zz_test_neow_placeholder_relic|zy_test_neow_implemented_relic' tests/use_cases/test_start_run.py` prints all three strings.
    - `rg -n 'test_auto_common_relic|test_auto_boss_relic|zz_test_auto_neow_relic' tests/use_cases/test_start_run.py` still prints matches, and their inclusion payloads use `"implementation_status": "implemented"`.
    - `uv run pytest -m guardrail tests/use_cases/test_start_run.py -q` exits 0.
  </acceptance_criteria>
  <done>Placeholder relics remain loadable as content metadata but no longer enter random standard/shop/boss/Neow runtime reward pools.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add loaded-vs-reachable report assertions</name>
  <files>tests/content/test_registry_validation.py</files>
  <read_first>
    - tests/content/test_registry_validation.py
    - src/slay_the_spire/use_cases/start_run.py
    - src/slay_the_spire/use_cases/opening_flow.py
    - src/slay_the_spire/content/catalog.py
    - src/slay_the_spire/content/provider.py
    - src/slay_the_spire/content/registries.py
    - content/characters/ironclad.json
    - content/acts/act1_map.json
    - content/acts/act2_map.json
    - .planning/phases/01-护栏与交付契约/01-CONTEXT.md
    - .planning/phases/01-护栏与交付契约/01-RESEARCH.md
  </read_first>
  <behavior>
    - `ReachabilityRow` records `content_type`, `content_id`, `loaded`, `reachable_via`, `status`, and `suggested_file`.
    - A helper formats failures as `"{content_type}:{content_id} status={status} reachable_via={... or -} check={suggested_file}"`.
    - The guardrail test proves `bash` is loaded and reachable via `starter_deck:ironclad`.
    - The guardrail test proves `anger` is loaded and reachable via `card_acquisition_tag:combat_reward`.
    - The guardrail test proves `fire_potion` is loaded and reachable via `potion_pool:starter_potions`.
    - The guardrail test proves `jaw_worm` is loaded and reachable via `act_enemy_pool:act1_basic`.
    - The guardrail test proves at least one placeholder relic is reported with `status="placeholder_report_only"` and no `runtime_relic_sequence:*` or `neow_random_pool` marker.
  </behavior>
  <action>In `tests/content/test_registry_validation.py`, add `from dataclasses import dataclass` and `from slay_the_spire.use_cases.start_run import start_new_run` if absent. Add a frozen dataclass:
```python
@dataclass(frozen=True)
class ReachabilityRow:
    content_type: str
    content_id: str
    loaded: bool
    reachable_via: tuple[str, ...]
    status: str
    suggested_file: str
```
Add `_format_reachability_failures(rows: list[ReachabilityRow]) -> str` returning newline-joined rows in the exact shape `"{row.content_type}:{row.content_id} status={row.status} reachable_via={','.join(row.reachable_via) or '-'} check={row.suggested_file}"`.

Add `_content_reachability_rows(provider: StarterContentProvider) -> list[ReachabilityRow]` using existing provider APIs:
- For cards: mark loaded cards reachable via `starter_deck:{character.id}` when in any character starter deck, and via `card_acquisition_tag:{tag}` for every tag in `card.acquisition_tags`.
- For relics: build `run_state = start_new_run("ironclad", seed=7, registry=provider)`, mark `runtime_relic_sequence:{pool_id}` for every relic ID in `run_state.relic_sequences[pool_id]`, mark `starter_relic:{character.id}` for character starter relics, mark `declared_relic_pool:{pool}` for every `relic.pools` entry, and mark `neow_random_pool` only for relics where `"neow" in relic.pools`, `relic.implementation_status != "placeholder"`, and the owner-character rule allows `ironclad`.
- For placeholder relics: set `status="placeholder_report_only"` only when `relic.implementation_status == "placeholder"` and no marker starts with `runtime_relic_sequence:` and no `neow_random_pool` marker exists.
- For potions: mark loaded potions reachable via `potion_pool:{pool_id}` by iterating `provider._catalog.potion_pool_ids` and `provider.potion_ids_for_pool(pool_id)`.
- For enemies and encounters: mark reachability via `act_enemy_pool:{act.enemy_pool_id}`, `act_elite_pool:{act.elite_pool_id}`, `act_boss_pool:{act.boss_pool_id}`, and `act_encounter_pool:{pool_id}` using `provider.enemy_pool_entries(...)` and `provider.encounter_pool_entries(...)`.
- For events: mark reachability via `act_event_pool:{act.event_pool_id}` using `provider.event_pool_entries(...)`.
If a non-placeholder row has no reachable markers, set `status="loaded_not_reachable"`; otherwise set `status="reachable"`.

Add `@pytest.mark.guardrail` test `test_content_reachability_report_distinguishes_loaded_and_reachable_content` that builds `StarterContentProvider(root / "content")`, indexes rows by `(content_type, content_id)`, and asserts the exact markers from the behavior block. The final assertion must fail on any non-placeholder `status == "loaded_not_reachable"` with `_format_reachability_failures(missing[:50])`.

Do not edit root `content/` or `src/slay_the_spire/data/content/` in this task.</action>
  <verify>
    <automated>uv run pytest -m guardrail tests/use_cases/test_start_run.py tests/content/test_registry_validation.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'class ReachabilityRow' tests/content/test_registry_validation.py` prints one match.
    - `rg -n 'loaded_not_reachable|placeholder_report_only|runtime_relic_sequence:|neow_random_pool|starter_deck:ironclad|card_acquisition_tag:combat_reward|potion_pool:starter_potions|act_enemy_pool:act1_basic' tests/content/test_registry_validation.py` prints matches for every listed string.
    - `uv run pytest -m guardrail tests/use_cases/test_start_run.py tests/content/test_registry_validation.py -q` exits 0.
  </acceptance_criteria>
  <done>Content coverage guardrail can show loaded vs reachable rows and readable missing-entry messages after placeholder runtime filtering is in place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local JSON content -> registry | Root `content/` metadata is parsed into trusted in-memory registries. |
| registry -> runtime reward pools | Loaded relic metadata controls player-facing random relic sequences and Neow relic offers. |
| test assertion output -> developer | Failure messages guide local developer changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Tampering | `content/` metadata | mitigate | Do not edit generated package content; tests load root `content/` via `StarterContentProvider` and assert exact loaded/reachable statuses. |
| T-01-02-02 | Information Disclosure | reachability failure output | accept | Output contains only local content IDs, pool names, and file hints; no secrets or PII exist in this local TUI phase. |
| T-01-02-03 | Repudiation | placeholder filtering behavior | mitigate | Add explicit guardrail tests for standard/shop/boss sequences and Neow selection so filtering is observable and attributable. |
| T-01-02-04 | Denial of Service | empty relic pools after filtering | mitigate | Existing fallback behavior in reward generation remains; tests verify current inclusion fixtures use implemented relics so pools are not emptied by placeholder-only fixtures. |
</threat_model>

<verification>
Run:
```bash
uv run pytest -m guardrail tests/use_cases/test_start_run.py tests/content/test_registry_validation.py -q
uv run pytest tests/use_cases/test_start_run.py tests/content/test_registry_validation.py -q
```
</verification>

<success_criteria>
GUARD-02 is satisfied when report rows distinguish loaded and reachable content, placeholder relics appear in readable report output, and no placeholder relic can be selected from runtime random relic sequences or Neow random relic offers.
</success_criteria>

<output>
After completion, create `.planning/phases/01-护栏与交付契约/01-02-SUMMARY.md`.
</output>
